{
  "name": "wordflowbot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "bot": "node --env-file=$WORDFLOWBOT_ENV_FILE --import tsx src/app/index.ts",
    "build": "tsc && vite build",
    "db:up": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE up --build",
    "db:down": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE down",
    "db:restart": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE restart postgres",
    "db:logs": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE logs -f postgres",
    "db:ps": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE ps",
    "db:sql:admin": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE exec postgres sh -lc 'PGPASSWORD=\"$POSTGRES_ADMIN_PASSWORD\" psql -h localhost -U \"$POSTGRES_ADMIN_USER\" -d \"$POSTGRES_DB\"'",
    "db:sql:app": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE exec postgres sh -lc 'PGPASSWORD=\"$APP_DB_PASSWORD\" psql -h localhost -U \"$APP_DB_USER\" -d \"$POSTGRES_DB\"'",
    "db:sql:migrator": "docker compose --env-file=$WORDFLOWBOT_ENV_FILE exec postgres sh -lc 'PGPASSWORD=\"$MIGRATION_DB_PASSWORD\" psql -h localhost -U \"$MIGRATION_DB_USER\" -d \"$POSTGRES_DB\"'",
    "preview": "vite preview",
    "prisma:generate": "node --env-file=$WORDFLOWBOT_ENV_FILE ./node_modules/prisma/build/index.js generate",
    "prisma:migrate": "node --env-file=$WORDFLOWBOT_ENV_FILE ./node_modules/prisma/build/index.js migrate dev",
    "prisma:migrate:deploy": "node --env-file=$WORDFLOWBOT_ENV_FILE ./node_modules/prisma/build/index.js migrate deploy",
    "prisma:migrate:status": "node --env-file=$WORDFLOWBOT_ENV_FILE ./node_modules/prisma/build/index.js migrate status",
    "test": "tsx --test tests/**/*.test.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.3",
    "prisma": "^7.8.0",
    "tsx": "^4.21.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "openai": "^6.34.0",
    "pg": "^8.20.0",
    "telegraf": "^4.16.3"
  }
}
